name: build

on:
  push:
    branches:
      - main
      - java-27
  pull_request:
    branches:
      - main

concurrency:
  group: build-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  test:
    name: maven test (${{ matrix.os }}, jdk 27)
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest
          - macos-latest
          - windows-latest

    steps:
      - name: checkout
        uses: actions/checkout@v5

      - name: set up jdk 27
        uses: actions/setup-java@v5
        with:
          # Temporary until Java 27 reaches GA on September 15.
          distribution: oracle-openjdk
          java-version: '27-ea'
          cache: maven

      - name: run local check target
        run: make check
