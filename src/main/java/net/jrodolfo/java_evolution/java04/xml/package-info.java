/**
 * Executable examples for JAXP/XML support in Java 4.
 *
 * <p>
 * This historical example uses the Java 4-era parser and DOM APIs with
 * trusted in-memory XML. XMLConstants and external-resource restrictions were
 * added in later JDK releases, so production code should apply the modern
 * hardening appropriate to its JDK rather than treating this example as a
 * complete security configuration.
 * </p>
 */
package net.jrodolfo.java_evolution.java04.xml;
