/**
 * Executable local examples for Java 4 integrated security APIs.
 *
 * <p>The implementation intentionally uses later secure algorithms and
 * compatibility APIs, such as AES-GCM, SHA-256, {@code GCMParameterSpec},
 * {@code Arrays.copyOf}, and UTF-8 constants, because the example runs on a
 * modern JDK and should not teach obsolete cryptography.</p>
 */
package net.jrodolfo.java_evolution.java04.security;
